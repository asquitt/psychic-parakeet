"""
Integration Tests Package

This package contains integration tests that verify the interaction
between multiple components of the ML pipeline system.

Test Categories:
- End-to-end pipeline tests (data → training → serving)
- API integration tests (FastAPI endpoints)
- MLflow integration tests (tracking and registry)
- Database integration tests (PostgreSQL)
- Deployment strategy tests (canary, blue/green, shadow)
"""
